import styled from "@emotion/styled";

export const LogoStyle = styled.div`
  .logoWrapper {
    display: grid;
    height: 100vh;
  }
  .logo {
    justify-self: center;
    align-self: center;
  }
`;
